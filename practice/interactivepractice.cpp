#include<cstdio>
#include<iostream>
#include<algorithm>
#include<vector>
#include<set>

using namespace std;

int main () {
	int t;
	cin>>t;
	while (t--) {
		int a,b;
		cin>>a>>b;
		int low=a,high=b;
		bool found=false;
		while(!found) {
			q=low+high;
			q/=2;
			cout<<q<<endl;
			if (q==)
		}
	}
}